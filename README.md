**Usage: EncryptionWidget <e|d> \<input> \[d]**

\<e|d> = Encryption mode (encrypt or decrypt).

\<input> = String input to be encrypted. If mode is f, you may use this as a filepath.

\[d] = Optional debug flag for verbose output.
